import React from "react";

export default (props) => {
  return (
    <Form.Group controlId="formBasicEmail">
      <Form.Label>Busca</Form.Label>
      <Form.Control type="email" placeholder="Nome da categoria" />
    </Form.Group>
  );
};
